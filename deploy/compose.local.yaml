# Opt in with COMPOSE_FILE=deploy/compose.yaml:deploy/compose.local.yaml in deploy/.env.
# Required when using local file storage or local sandbox workspaces in Docker.
services:
  wave:
    volumes:
      - local_data:/var/lib/wave
volumes:
  local_data:
